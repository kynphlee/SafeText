<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent" 
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@drawable/safetext_background"
    tools:context=".STSignup">

    <ImageView
        android:id="@+id/passive_shield"
        android:layout_width="128dp"
        android:layout_height="128dp"
        android:layout_centerHorizontal="true"
        android:src="@drawable/st_passive"/>
    <TextView 
        android:id="@+id/safetext_summary"
        style="@style/UIText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/passive_shield"
        android:layout_marginTop="25dp"
        android:textSize="25sp"
        android:text="@string/st_welcome"/>
    <TextView
        android:id="@+id/sign_in_text"
        style="@style/UIText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="17sp"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/safetext_summary"
        android:layout_marginTop="15dp"
        android:text="@string/st_signin_text"/>
    <EditText 
        android:id="@+id/pin_field"
        android:inputType="textCapWords"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:textColor="#FFFFFF"
        android:layout_below="@id/sign_in_text"
        android:drawableLeft="@drawable/st_pin_logo"
        android:layout_marginTop="16dp"
        android:hint="@string/pin"
        android:textColorHint="#DDDDDD"/>
    <ImageView
        android:id="@+id/activate_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/pin_field"
        android:layout_marginTop="30dp"
        android:src="@drawable/st_activate_button" />

</RelativeLayout>
